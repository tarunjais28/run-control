# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib

# Test binary, build with `go test -c`
*.test

# Output of the go coverage tool, specifically when used with LiteIDE
*.out
# Ignore all build artefacts across product
**/target/

# Remove Cargo.lock from gitignore if creating an executable, leave it for libraries
# More information here https://doc.rust-lang.org/cargo/guide/cargo-toml-vs-cargo-lock.html
Cargo.lock

# These are backup files generated by rustfmt
**/*.rs.bk

# Ignore the idea folder across product
**/.idea

# Ignore test-beds across products. 
# Outputs of each app go in a folder called test-bed. This makes it easy to write build scripts that refer to a test-bed location.
**/test-bed/
